name: mediku_web_demo
description: "Web demo wrapper for Mediku mobile app"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  device_preview: ^1.2.0
  sqflite_common_ffi_web: ^0.4.2+9
  
  # Import the main Mediku app
  mediku:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
