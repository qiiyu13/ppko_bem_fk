name: mediku_web_demo
description: "Web demo importing real Mediku app with mock services"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  device_preview: ^1.2.0
  device_frame: ^1.4.0
  uuid: ^4.5.3
  
  # Import the main Mediku app
  mediku:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
